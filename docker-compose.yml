# base Docker Compose file
# use with environment-specific overrides:
#   dev: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
#   prod: docker compose -f docker-compose.yml -f docker-compose.prod.yml up

x-db-env: &db-env
  DB__HOST: ${DB__HOST}
  DB__PORT: ${DB__PORT}
  DB__USERNAME: ${DB__USERNAME}
  DB__PASSWORD: ${DB__PASSWORD}
  DB__NAME: ${DB__NAME}

x-common-env: &common-env
  ENV: ${ENV}
  REGIONS: ${REGIONS}

services:
  db:
    image: timescale/timescaledb-ha:pg16-ts2.26
    environment:
      POSTGRES_USER: ${DB__USERNAME}
      POSTGRES_PASSWORD: ${DB__PASSWORD}
      POSTGRES_DB: ${DB__NAME}
    restart: always
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d
    networks: [nn_network]
    healthcheck:
      test: |
        psql -U $DB__USERNAME -d $DB__NAME \
        -c "SELECT 1" > /dev/null || exit 1
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
      target: api
    environment:
      <<: [*common-env, *db-env]
      # use __ nested env delimiter syntax for nested pydantic-settings objects
      # TODO: remove sensitive vars set as secret w/ pydantic-settings v2.12
      API__ADMIN_USERNAME: ${API__ADMIN_USERNAME}
      API__ADMIN_PASSWORD: ${API__ADMIN_PASSWORD}
    depends_on:
      db:
        condition: service_healthy
    networks: [nn_network]
    healthcheck:
      # TODO: create a healthcheck endpoint, just using root for now
      test: ["CMD", "curl", "-f", "http://localhost:${API__PORT:-8000}/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

  prefect-server:
    image: prefecthq/prefect:3-python3.12
    environment:
      PREFECT_API_URL: http://prefect-server:${PREFECT__PORT:-4200}/api
      PREFECT_UI_API_URL: http://127.0.0.1:${PREFECT__H_PORT:-4200}/api
      PREFECT_SERVER_DATABASE_CONNECTION_URL: postgresql+asyncpg://${DB__USERNAME}:${DB__PASSWORD}@${DB__HOST}:${DB__PORT}/prefect
      PREFECT_RESULTS_PERSIST_BY_DEFAULT: "true"
    depends_on:
      db:
        condition: service_healthy
    restart: always
    networks: [nn_network]
    command: prefect server start --host 0.0.0.0
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:4200/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  prefect-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
      target: worker
    environment:
      <<: [*common-env, *db-env]
      PREFECT_API_URL: http://prefect-server:${PREFECT__PORT:-4200}/api
      PREFECT_HOME: /backend/.prefect
      PREFECT_LOGGING_SETTINGS_PATH: /backend/core/logging/logging.prefect.yaml
      POOL_NAME: forecasts
    depends_on:
      db:
        condition: service_healthy
      prefect-server:
        condition: service_healthy
    restart: always
    networks: [nn_network]

volumes:
  pg_data:


networks: 
  nn_network:
    driver: bridge

# TODO: roll-out secrets with new release for pydantic-settings v.2.12!
#
# secrets:
#   db__username:
#     environment: "DB__USERNAME"
#   db__password:
#     environment: "DB__PASSWORD"
#   api__admin_username:
#     environment: "API__ADMIN_USERNAME"
#   api__admin_password:
#     enviroment: "API__ADMIN_PASSWORD"
  


