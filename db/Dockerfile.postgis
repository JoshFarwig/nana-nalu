# Platform-agnostic PostgreSQL + PostGIS build
# Builds natively for both amd64 and arm64 (Ampere A1)
FROM postgres:18-alpine

# Install PostGIS from Alpine packages
# Note: Alpine includes all PostGIS extensions in the base package
RUN apk add --no-cache postgis
